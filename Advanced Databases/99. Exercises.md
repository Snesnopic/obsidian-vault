# 99. Exercises

## Midterm - 9th of April 2024

**1. Consider a schema `R(IdR, A, B, ...)`, `S(IdS, A, C, ...)`, `T(IdT, A, D, ...)` and the following query**:

```sql
SELECT DISTINCT r.A, SUM(r.B), SUM(s.C), SUM(t.D)
FROM R r, S s, T t
WHERE r.A = s.A AND s.A = t.A
GROUP BY r.A
````

Assume that R, S, and T primary keys are `R.IdR`, `S.IdS`, and `T.IdT`. Assume that unclustered RID-sorted indexes are defined on all the primary keys and on `R.A`, `S.A`, and `T.A`.

Assume the following table for the optimization parameters. If you need Cardenas formula $\Phi(n,k)$, approximate it with $\min(n,k)$. Assume that attributes are 4 bytes long and that pages are 4K. Assume a buffer of 100 pages. When the numbers get complicated, feel free to approximate (e.g., 3.724.735 ~ 3.720.000).

|**Table / Index**|**NReg**|**NPag**|**NLeaf of Indexes**|**NKey**|**Min**|**Max**|
|---|---|---|---|---|---|---|
|**R**|100.000|1.000|120||||
|**S**|100.000|2.000|120||||
|**T**|20.000|300|21||||
|**Idx.R.A**|||See R|10.000|0|100.000|
|**Idx.S.A**|||See S|10.000|0|100.000|
|**Idx.T.A**|||See T|500|0|100.000|

**Questions:**

- **a)** How many records are returned by the join (that is, the last join that is performed before the 'group by')? How many pages, after one projects the result over the fields that are needed by the GROUP BY?
    
- **b)** Give an estimate of the number of groups generated by the GROUP BY.
    
- **c)** Compute the cost of an access plan that first uses IndexNestedLoop on S Join R (S external), and then a MergeJoin on the result with T, and finally a GroupBy.
    
- **d)** The access plan of the previous point is unreasonable; suggest a reasonable access plan but do not compute its cost.
    
- **e)** Is "GROUP BY" redundant in this query?
    

---

**2. Consider the table of exercise (1) and evaluate the size of the following data structures:**

- **a.** An inverted-list index on attribute A on table R
    
- **b.** A bitmax index on attribute A on table R
    

---

**3. Consider the following log content.**

Assume that the DB was identical to the buffer before the beginning of this log, and consider undo / redo protocol:

`(begin,T1) (W,T1,A,1,30) (W,T1,B, 1,50) (begin, T2) (W, T2, C,1,40) (begin-ckp, {T1,T2}) (begin, T3) (W,T3,B,50,70) (commit,T1) (end-ckp) (begin, T4) (commit,T2) (W,T4,C,40,80)`

**Questions:**

- **a.** Before starting this log, what was the content of A, B and C in the PS (Persistent Store)?
    
- **b.** Assume there was a crash at the end of the logging period. At crash time, what was the content of A, B and C in the buffer? What can be said about the content of A, B and C in the PS?
    
- **c.** At restart time, which transactions are redone and which are undone?
    
- **d.** List the operations that are undone, in the order in which are undone.
    
- **e.** List the operations that are redone, in the order in which are redone.
    
- **f.** After restart is finished, what is the content of A, B and C in the buffer?
    
- **g.** After restart is finished, what is the content of A, B and C in the PS?
    
- **h.** What could be said about the content of A, B and C in the PS at crash time if we adopted instead a undo / no-redo protocol? Why (one-sentence explanation)?
    
- **i.** What could be said about the content of A, B and C in the PS at crash time if we adopted instead a no-undo / redo protocol? Why (one-sentence explanation)?



**1. Consider a schema `R(IdR, A, ..., IdS*)`, `S(IdS, B, ...)` and the following query**:

```sql
SELECT DISTINCT r1.A, count(*)
FROM R r, S s
WHERE r.IdS = s.IdS AND 50.000 <= r.A < 70.000 AND s.B < 1.000
GROUP BY r1.A, s.B
````

Assume that R and S primary keys are `R.IdR` and `S.IdS`, while `R.IdS` is a foreign key. Assume that unclustered RID-sorted indexes are defined on all the primary and foreign keys and on `R.A`. Assume that the index on `s.B` is clustered.

Assume the following table for the optimization parameters. If you need Cardenas formula $\Phi(n,k)$, approximate it with $\min(n,k)$.

Assume that each value of `R.A` is chosen among the values of `S.B`.

|**Table / Index**|**NReg**|**NPag**|**NLeaf of Indexes**|**NKey**|**Min**|**Max**|
|---|---|---|---|---|---|---|
|**R**|10.000.000|100.000|20.000||||
|**S**|1.000.000|20.000|2.000||||
|**Idx.R.A**|||See R|10.000|0|100.000|
|**Idx.S.B**|||See S|1.000|0|10.000|

**Questions:**

- **a)** How many records are returned by the join (that is, the last join that is performed after the restrictions and before the 'group by')? How many pages? If needed, assume that every page is 4000 bytes long. Observe that, for any file, $DPag \times NPag = LRec \times NRec$.
    
- **b)** Compute the cost of an access plan that first uses IndexNestedLoop on r1-r2 and then a SortMerge on the result with S. Please REUSE THE RESULT OF QUESTION (a). _(Nota: il testo originale riporta "r1-r2", probabile refuso per indicare la tabella R o una self-join implicita non presente nella query formattata)._
    
- **c)** Is "DISTINCT" redundant in this query?
    
- **d)** Starting from the result of question A, give an estimate of the number of groups generated by the GROUP BY if it were changed to "GROUP BY r1.A"
    
- **e)** Give an estimate of the number of groups generated by the GROUP BY above, that is, “GROUP BY r1.A, s.B”

2. Give the total cost of the MergeSort/HashJoin algorithm among two table T1 and T2 in the typical situation when $B$ < NPag < $B^2$ for both tables, **including the cost of reading the input and writing the output.** Now describe the cost of the same algorithm when $B^2$ < NPag < $B^3$ for T2, while $B$ < NPag < $B^2$ for T1, still including the cost of reading the input and writing the output